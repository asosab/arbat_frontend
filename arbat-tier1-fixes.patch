diff --git a/_includes/footer.html b/_includes/footer.html
index 608c32c..26cf6c4 100644
--- a/_includes/footer.html
+++ b/_includes/footer.html
@@ -12,6 +12,15 @@
       </p>
       <p><a href="{{ site.arbat.instagram }}" target="_blank" rel="noopener">Instagram: @arbat.archery</a></p>
     </div>
+    <nav class="footer-nav" aria-label="Enlaces del sitio">
+      <a href="{{ '/clases/principiantes/' | relative_url }}">Clases</a>
+      <a href="{{ '/precios/' | relative_url }}">Precios</a>
+      <a href="{{ '/horarios/' | relative_url }}">Horarios</a>
+      <a href="{{ '/instructores/raul-suarez/' | relative_url }}">Instructores</a>
+      <a href="{{ '/ubicacion/' | relative_url }}">Ubicación</a>
+      <a href="{{ '/preguntas-frecuentes/' | relative_url }}">Preguntas frecuentes</a>
+      <a href="{{ '/reservar/' | relative_url }}">Reservar</a>
+    </nav>
     <p class="footer-copy">© {{ 'now' | date: '%Y' }} {{ site.arbat.nombre }}. Todos los derechos reservados.</p>
   </div>
 </footer>
diff --git a/_includes/head.html b/_includes/head.html
index 3210c25..2690cf6 100644
--- a/_includes/head.html
+++ b/_includes/head.html
@@ -19,4 +19,5 @@
 <meta name="msapplication-config" content="{{ '/assets/images/favicon/browserconfig.xml' | relative_url }}">
 <meta name="msapplication-TileColor" content="#FF4F00">
 <link rel="stylesheet" href="{{ '/assets/css/style.css' | relative_url }}">
+<script src="{{ '/assets/js/nav.js' | relative_url }}" defer></script>
 {% seo title=false %}
diff --git a/_includes/header.html b/_includes/header.html
index 2b729f9..cc299ff 100644
--- a/_includes/header.html
+++ b/_includes/header.html
@@ -8,7 +8,7 @@
     <a class="site-logo" href="{{ '/' | relative_url }}">
       <img src="{{ '/assets/images/logo.png' | relative_url }}" alt="{{ site.arbat.nombre }}">
     </a>
-    <nav class="site-nav" aria-label="Navegación principal">
+    <nav class="site-nav" id="site-nav" aria-label="Navegación principal">
       <a href="{{ '/clases/principiantes/' | relative_url }}">Clases</a>
       <a href="{{ '/precios/' | relative_url }}">Precios</a>
       <a href="{{ '/horarios/' | relative_url }}">Horarios</a>
@@ -19,5 +19,8 @@
     {% assign wa_numero = site.arbat.telefono_whatsapp | first | remove: '+' | remove: ' ' %}
     {% assign wa_mensaje = 'Hola ARBAT, quiero información sobre las clases de tiro con arco.' | url_encode %}
     <a class="nav-cta" href="https://wa.me/{{ wa_numero }}?text={{ wa_mensaje }}" target="_blank" rel="noopener">WhatsApp</a>
+    <button class="nav-toggle" id="nav-toggle" type="button" aria-expanded="false" aria-controls="site-nav" aria-label="Abrir menú">
+      <span></span><span></span><span></span>
+    </button>
   </div>
 </header>
diff --git a/_layouts/clase.html b/_layouts/clase.html
index 8590add..b52eb18 100644
--- a/_layouts/clase.html
+++ b/_layouts/clase.html
@@ -6,6 +6,7 @@
 <body>
 {% include header.html %}
 <main>
+<div class="wrap wrap--page">
 {% include breadcrumbs.html %}
 <article class="clase">
   <h1>{{ page.title }}</h1>
@@ -17,6 +18,7 @@
     <a class="btn-primary" href="https://wa.me/{{ wa_numero }}?text={{ wa_mensaje }}" target="_blank" rel="noopener"><span class="ring-icon" aria-hidden="true"></span>Reservar</a>
   </p>
 </article>
+</div>
 </main>
 {% include footer.html %}
 {% include schema/service.html %}
diff --git a/_layouts/instructor.html b/_layouts/instructor.html
index c323832..e2d1229 100644
--- a/_layouts/instructor.html
+++ b/_layouts/instructor.html
@@ -6,6 +6,7 @@
 <body>
 {% include header.html %}
 <main>
+<div class="wrap wrap--page">
 {% include breadcrumbs.html %}
 <article class="instructor">
   <div class="instructor-media">
@@ -28,6 +29,7 @@
     <a class="btn-primary" href="https://wa.me/{{ wa_numero }}?text={{ wa_mensaje }}" target="_blank" rel="noopener"><span class="ring-icon" aria-hidden="true"></span>Escribir a {{ page.nombre }} por WhatsApp</a>
   </p>
 </article>
+</div>
 </main>
 {% include footer.html %}
 {% include schema/profile-page.html %}
diff --git a/assets/css/style.scss b/assets/css/style.scss
index 0b6d105..7616427 100644
--- a/assets/css/style.scss
+++ b/assets/css/style.scss
@@ -146,6 +146,14 @@ p { color: var(--grafito); margin-bottom: 16px; max-width: 640px; }
 
 .nav-cta { padding: 10px 20px; gap: 8px; font-size: 14px; }
 
+// Cuando .btn-primary se usa en un <button> (ej. formulario de reserva) en
+// vez de un <a>, hay que anular el estilo nativo del navegador para que
+// se vea idéntico al resto de los CTAs.
+button.btn-primary {
+  border: none;
+  cursor: pointer;
+}
+
 .ring-icon {
   width: 16px;
   height: 16px;
@@ -232,13 +240,32 @@ p { color: var(--grafito); margin-bottom: 16px; max-width: 640px; }
 // header.html debe usar estas clases (o renombrar aquí si preferís otras)
 // ---------------------------------------------------------------------------
 .site-header {
+  border-bottom: 1px solid var(--borde);
+}
+
+// Barra superior de teléfonos (arriba del todo, fuera de .wrap: se le da
+// el mismo padding lateral que .wrap a mano porque no comparte esa clase).
+.header-contacto {
+  padding: 10px 40px 0;
+  text-align: right;
+  font-size: 12px;
+}
+
+.header-contacto a {
+  color: var(--grafito);
+  text-decoration: none;
+}
+
+.header-contacto a:hover { text-decoration: underline; }
+
+// Fila principal: logo + nav + CTA de WhatsApp + toggle mobile
+.header-fila {
   display: flex;
   align-items: center;
   justify-content: space-between;
   flex-wrap: wrap;
   gap: 16px;
-  padding: 20px 0;
-  border-bottom: 1px solid var(--borde);
+  padding: 14px 0 20px;
 }
 
 .site-header .site-logo img {
@@ -261,24 +288,59 @@ p { color: var(--grafito); margin-bottom: 16px; max-width: 640px; }
   white-space: nowrap;
 }
 
-// WhatsApp + teléfono: viven FUERA de .site-nav para poder mantenerlos
-// visibles en mobile aunque se oculten los links de texto (ver visual.md §3.1)
-.site-header__contact {
-  display: flex;
-  align-items: center;
-  gap: 12px;
-  margin-left: auto;
+// Botón hamburguesa — oculto en desktop, .nav-cta (WhatsApp) siempre visible
+.nav-toggle {
+  display: none;
+  flex-direction: column;
+  justify-content: center;
+  gap: 5px;
+  width: 32px;
+  height: 32px;
+  padding: 0;
+  border: none;
+  background: none;
+  cursor: pointer;
+  flex-shrink: 0;
 }
 
-.site-header__phone {
-  font-size: 13px;
-  color: var(--grafito);
-  white-space: nowrap;
+.nav-toggle span {
+  display: block;
+  width: 100%;
+  height: 2px;
+  border-radius: 2px;
+  background: var(--tinta);
+  transition: transform .25s ease, opacity .2s ease;
 }
 
+.nav-toggle[aria-expanded="true"] span:nth-child(1) { transform: translateY(7px) rotate(45deg); }
+.nav-toggle[aria-expanded="true"] span:nth-child(2) { opacity: 0; }
+.nav-toggle[aria-expanded="true"] span:nth-child(3) { transform: translateY(-7px) rotate(-45deg); }
+
 @media (max-width: 720px) {
-  .site-nav { display: none; }
-  .site-header__contact { margin-left: auto; }
+  .header-contacto { padding: 8px 22px 0; text-align: center; }
+
+  .nav-toggle { display: flex; order: 3; }
+
+  // El link de WhatsApp queda visible siempre en mobile (ver nota arriba);
+  // el toggle se ubica después. La nav pasa a ser un desplegable a todo
+  // el ancho, debajo de la fila logo/CTA/toggle.
+  .site-nav {
+    display: none;
+    order: 4;
+    flex-basis: 100%;
+    flex-direction: column;
+    align-items: stretch;
+    gap: 0;
+    margin-top: 12px;
+    border-top: 1px solid var(--borde);
+  }
+
+  .site-nav.is-open { display: flex; }
+
+  .site-nav a {
+    padding: 14px 4px;
+    border-bottom: 1px solid var(--borde);
+  }
 }
 
 // ---------------------------------------------------------------------------
@@ -305,6 +367,57 @@ p { color: var(--grafito); margin-bottom: 16px; max-width: 640px; }
 .site-footer a { text-decoration: none; }
 .site-footer a:hover { text-decoration: underline; }
 
+.footer-nav {
+  display: flex;
+  flex-wrap: wrap;
+  gap: 8px 20px;
+  padding-top: 8px;
+  margin-top: 8px;
+  border-top: 1px solid var(--borde);
+}
+
+// ---------------------------------------------------------------------------
+// Formulario de reserva (/reservar/)
+// ---------------------------------------------------------------------------
+.formulario-reserva {
+  display: flex;
+  flex-direction: column;
+  gap: 20px;
+  max-width: 480px;
+  margin: 32px 0 40px;
+}
+
+.formulario-reserva label {
+  display: flex;
+  flex-direction: column;
+  gap: 8px;
+  font-family: 'Quicksand', sans-serif;
+  font-size: 14px;
+  font-weight: 600;
+  color: var(--tinta);
+}
+
+.formulario-reserva input {
+  font-family: 'Work Sans', sans-serif;
+  font-size: 15px;
+  font-weight: 400;
+  color: var(--tinta);
+  border: 1px solid var(--borde);
+  border-radius: 8px;
+  padding: 12px 14px;
+  background: var(--papel);
+}
+
+.formulario-reserva input:focus {
+  outline: 2px solid var(--diana);
+  outline-offset: 1px;
+}
+
+.formulario-reserva button {
+  align-self: flex-start;
+  margin-top: 4px;
+}
+
 // ---------------------------------------------------------------------------
 // Breadcrumbs
 // ---------------------------------------------------------------------------
diff --git a/assets/js/nav.js b/assets/js/nav.js
new file mode 100644
index 0000000..34c9724
--- /dev/null
+++ b/assets/js/nav.js
@@ -0,0 +1,23 @@
+// Toggle del menú de navegación en mobile.
+// El nav ya está en el HTML (accesible sin JS); esto solo controla si se
+// muestra u oculta con el botón hamburguesa. Sin dependencias.
+(function () {
+  document.addEventListener("DOMContentLoaded", function () {
+    var toggle = document.getElementById("nav-toggle");
+    var nav = document.getElementById("site-nav");
+    if (!toggle || !nav) return;
+
+    toggle.addEventListener("click", function () {
+      var abierto = nav.classList.toggle("is-open");
+      toggle.setAttribute("aria-expanded", abierto ? "true" : "false");
+    });
+
+    // Cerrar el menú al elegir un link, para no tener que hacerlo a mano.
+    nav.addEventListener("click", function (evento) {
+      if (evento.target.tagName === "A") {
+        nav.classList.remove("is-open");
+        toggle.setAttribute("aria-expanded", "false");
+      }
+    });
+  });
+})();
diff --git a/reservar.md b/reservar.md
index 2a97b43..4c7d4dd 100644
--- a/reservar.md
+++ b/reservar.md
@@ -22,7 +22,7 @@ Completá el formulario y te mandamos la solicitud lista por WhatsApp.
   <label for="reserva-edades">Edad de cada persona
     <input type="text" id="reserva-edades" name="edades" placeholder="Ej.: 25, 7" required>
   </label>
-  <button type="submit">Enviar por WhatsApp</button>
+  <button type="submit" class="btn-primary"><span class="ring-icon" aria-hidden="true"></span>Enviar por WhatsApp</button>
 </form>
 
 <script>
