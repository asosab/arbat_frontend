/**
 * Buddy WhatsApp Listener — configuración del módulo.
 *
 * El módulo se carga automáticamente por buddy.js. No depende de
 * data-buddy-abilities.
 */
window.BuddyWaListenerConfig = window.BuddyWaListenerConfig || {};
window.BuddyWaListenerConfig = Object.assign({
  enabled: true
}, window.BuddyWaListenerConfig || {});
